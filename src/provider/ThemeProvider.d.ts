import { type ReactNode } from "react";
type ThemeProviderProps = {
    children: ReactNode;
};
declare const ThemeProvider: ({ children }: ThemeProviderProps) => import("react/jsx-runtime").JSX.Element;
export default ThemeProvider;
